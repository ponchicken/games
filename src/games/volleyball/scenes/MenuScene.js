import { SCENE } from '../constants'
import Phaser from 'phaser'

export class MenuScene extends Phaser.Scene {
  constructor() {
    super({
      key: SCENE.menu
    })
  }

  // public create () {}
}
